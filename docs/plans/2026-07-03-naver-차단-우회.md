# 설계 노트 — Naver 차단 우회 (KR 리포트 데이터 소스 전환) + 주간 GAS pre-fetch

2026-07-03. `/design-check` 첫 적용 (pending "설계 게이트 2단계" 증명 건).

## 배경 (실측)
- KR 리포트 에이전트(GH Actions 러너의 Claude CLI WebFetch)가 Naver 접근 실패 14일째(6/19경~). 거래대금·업종 강세/약세·종목 수급·뉴스 원인 확인·종목 자동선별 전부 degrade.
- 로컬(한국 IP) curl: `m.stock.naver.com/api/index/KOSPI/basic`·`finance.naver.com/sise/sise_group.naver`·`api/stock/005930/basic` **전부 200** (2026-07-03 실측) → Naver 서비스 정상, **러너(해외 DC IP) 측 차단** 유력.
- 6/28 주간 리포트: 워크플로 로그에 `GAS_WEB_APP_URL: ***` 주입 확인됐는데 에이전트가 "환경변수 미설정" 판정 → dailyReturns 역산 추산. 에이전트 판단 의존이 단일 실패점.

## 1. 외부 동작 가정 + 근거

| # | 가정 | 근거 / 상태 |
|---|---|---|
| 1 | GH 러너에서 Naver가 403/차단 페이지를 반환한다 (에이전트 "차단" 보고의 실체) | **[검증 필요]** → A-1 진단 워크플로로 HTTP 코드+응답 첫 200바이트 실측. 200+차단페이지(CAPTCHA) 가능성도 응답 본문으로 판별 |
| 2 | 연합인포맥스·한경도 러너에서 차단 여부 불명 (리포트 "뉴스 차단"이 Naver만인지 전체인지 미분리) | **[검증 필요]** → A-1에 포함 |
| 3 | GAS `UrlFetchApp`(Google egress IP) → Naver 허용 | **[검증 필요]** → GAS diag action으로 실측. Google DC IP도 차단 가능성 있음 — 허용이어도 **장기 취약**(러너 차단과 같은 부류) → 뉴스 프록시 후보일 뿐, 시세·수급의 1차 경로로 삼지 않음 |
| 4 | KIS 공식 API에 업종별 시세·투자자매매동향 TR 존재 (인증 API라 IP 차단 부류 원천 회피) | 국내주식 시세 TR군에 업종지수(`FHPUP02100000` 계열)·구분별전체시세·투자자매매동향(시장별/종목별 `FHKST01010900`) 존재로 앎 — **정확한 tr_id·URL·파라미터는 [검증 필요]** → GAS에서 시험 호출 스모크 |
| 5 | KIS 지수 응답에 누적 거래대금 필드(`acml_tr_pbmn` 계열) 포함 → "거래대금 —" 해소 | **[검증 필요]** → 같은 스모크에서 필드 확인 |
| 6 | KIS rate limit(초당 건수)은 리포트 1일 1회 수 콜에 무관, 기존 `getToken()` 재사용으로 토큰 충돌 없음 | 기존 `updateAllNew`가 fetchAll 5개 throttle로 상시 운용 중인 사실로 뒷받침. 시각도 17:05(장 마감 후)라 장중 푸시와 분리 |
| 7 | doPost에 action 분기 추가는 기존 라우팅(webhook·pushPnL 등) 불변 | Main.js:76 doPost 구조 확인 — action별 early return, 신규 분기는 기존 분기 뒤에 추가. 코드로 검증됨 |
| 8 | 고정 배포는 push만으론 갱신 안 됨 → Apps Script API로 버전 생성+배포 갱신 필요 (배포 3개) | memory `reference_gas_redeploy_via_api` + 과거 v12 검증 절차로 확립 |
| 9 | weekly job에서 curl로 GAS portfolioMetrics pre-fetch → JSON 파일 저장 → 에이전트는 Read만 | 일일 KR 에이전트가 같은 curl 패턴으로 매일 성공 중(파이프라인 GAS 지표 기록으로 입증). step 레벨 curl은 에이전트 판단 개입 자체가 없음 |

## 2. 과거 부류 매칭 (errors.md)

- **Cloudflare PoP IP → GAS 403 (2026-05-31, 영구 불가 확정)** + **routine 환경 → GAS/Worker/Yahoo 차단 (2026-06-02~03)**: *DC IP에 대한 anti-abuse 차단은 코드·헤더로 우회 불가, 인프라 선택으로만 회피* — 이번 설계의 핵심 결정(비인증 스크래핑 → **인증 공식 API(KIS)**)이 이 교훈의 직접 적용. Naver를 GAS 프록시로 우회하는 안은 같은 부류의 재발 위험이 있어 **뉴스 한정 + 차단 시 degrade 명시**로 제한.
- **"로컬 curl 통과"로 실환경 판정한 오진단 (routine 사건에서 반복)**: 로컬 200은 Naver 정상의 증거일 뿐 러너 상태의 증거가 아님 → 러너 실측(A-1)을 구현 착수 조건으로 못박음.
- **"Version N already deployed" 착시**: 배포 후 반드시 배포 버전·시각을 API로 확인(가정 8 절차에 포함).
- **getUi() in 에디터 실행 함수**: 신규 GAS 코드는 Logger.log만 사용.
- **clasp push 금지**: push_safe.py 경유 (Secret.js 보호).

## 3. 추론 가능 vs 실환경 전용

**머리로 거름 (여기서 확정)**:
- doPost 분기 추가의 라우팅 안전성 (early return 구조)
- secret 게이트: `marketSnapshot`·`diagMarket` 모두 `TG_WEBHOOK_SECRET` 검증 (기존 `portfolioMetrics` 패턴 복제) — 비인증 노출 없음
- 응답에 원화 절대액 미포함 원칙 유지 (기존 portfolioMetrics 규칙 승계)
- weekly pre-fetch JSON은 `/tmp` 경로 → commit 대상( docs/reports/ ) 밖 — 리포트 커밋에 딸려 들어가지 않음
- KIS 호출 시각(17:05 KST)은 장 마감 후 — 당일 확정치 조회 가능

**실환경에서만 (스모크/실측으로만 판정)**:
- 러너→Naver·인포맥스·한경 실제 HTTP 코드/본문 (가정 1·2) → **A-1 진단 워크플로**
- GAS→Naver 허용 여부 (가정 3) → **GAS diagMarket 실측**
- KIS 업종/투자자 TR의 정확한 id·파라미터·응답 스키마 (가정 4·5) → **GAS 시험 호출** (문서 기억만으로 코딩 확정 금지 — 시험 호출 응답을 보고 파싱 작성)
- 전환 후 리포트 품질 (거래대금 숫자·업종 섹션 복원) → **dry_run 게이트**

## 4. 검증 방법 (사전 정의)

**자동 (Claude)**:
1. A-1 진단: `diag-egress.yml` workflow_dispatch 1회 → 로그에서 소스별 `HTTP <code> + 본문 head` — 예상: Naver 403 또는 200+차단페이지, Yahoo 200(대조군)
2. GAS 스모크: `diagMarket` action curl(로컬) → KIS 업종·투자자·지수 응답 JSON 필드 실물 확인 / Naver fetch 응답 코드
3. push_safe `node --check` 통과 + Apps Script API로 배포 3개 버전·시각 확인 (push 시각보다 이후)
4. `marketSnapshot` curl → walk-through: KOSPI 지수·등락%·거래대금(조 단위 환산)·업종 상위/하위 3·보유 5종 외인/기관 순매수 필드가 전부 숫자로 존재
5. KR 리포트 dry_run dispatch → 리포트 본문에 ① 거래대금 "—" 아님 ② 업종 강세/약세 섹션 실데이터 ③ "Naver 차단 N일차" 문구 소멸
6. weekly dry_run → dailyReturns가 "역산 추산" 아닌 GAS 수치로 표기, pre-fetch step 로그에 HTTP 200

**사용자**:
- 다음 거래일 17:05 텔레그램 KR 리포트에서 **시장 스냅샷 표의 거래대금**이 숫자(예: "12.3조")로, **자금 흐름 섹션**이 업종 실데이터로 표시되는지 확인 → 예상: "—(Naver 차단)" 문구 완전 소멸

## 구현 순서 (게이트 통과 시)
1. **A-1**: `diag-egress.yml` 추가(dispatch 전용, curl만) → dispatch → 실측 → 결과로 뉴스 경로 확정
2. **A-2a**: GAS `diagMarket`(임시, secret 게이트) push+재배포 → KIS TR 스모크 → 응답 스키마 확보
3. **A-2b**: `marketSnapshot` 본구현(KIS 업종/수급/지수/거래대금 + 뉴스 경로) + `diagMarket` 제거 → push+재배포+버전확인
4. **A-3**: `kr-prompt.md` 소스 전환(1차 GAS marketSnapshot, Yahoo 폴백 유지) → dry_run 게이트
5. **B**: weekly job pre-fetch step + `weekly-prompt.md` Read 방식 전환 → dry_run
6. code-map.md·api-reference.md·pending.md 갱신

→ **게이트: 통과** — 단 [검증 필요] 5건(가정 1~5)이 전부 구현 *앞단*의 A-1·A-2a 스모크로 배치되어 있고, 그 결과가 나오기 전에는 본구현(A-2b 이후) 코딩을 시작하지 않는다.
