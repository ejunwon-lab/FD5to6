# 2026-05-25 — Trade Log (ActivityPage) 전면 재설계

## 배경

다른 창에서 Indicators 카테고리 그룹화 작업 완료 후 (commit `0b5f679`),
이어서 Trade Log 점검 중 API surrogate 에러로 그 창 중단 → 이 창에서 이어감.

## 발견한 문제 (3가지)

### 1. 사일런트 버그 — 응답 키 미스매치

| 쪽 | 키 |
|---|---|
| GAS 응답 (`newMobileGetMonthlyRealized`) | `monthly` |
| 데스크 (`DataProvider.refreshRealized`) | `entries` |

→ `res.entries ?? []` 가 항상 빈 배열 → SAMPLE_REALIZED fallback만 표시.
사용자가 보던 표는 **실제 매매 데이터가 아니라 샘플**.

### 2. GAS endpoint가 월별 집계만 반환

실현손익 시트는 14컬럼 풀 데이터 (매도일·종목코드·종목명·분류·증권사·계좌·매도수량·매도단가·매도금액·평균매입단가·매입원가·수수료·실현손익·수익률) 보유.

그런데 endpoint는 `{month, count, winCount, profit, profitRate, winRate}` 6필드 집계만 반환.
→ 종목·매도일·증권사 등 *건별 회고*에 필요한 행 단위 정보가 데스크에 도달 못 함.

흥미로운 점: **데스크 타입(`MonthlyRealizedItem`)은 이미 "행 단위" 모양으로 설계**되어 있었음.
GAS만 잘못 구현된 케이스.

### 3. 표시 규칙 위반

- 테이블 첫 컬럼 "Symbol"(종목코드 메인) → memory `feedback_stock_name_primary` 위반
- 월별 막대 라벨 `+45만` 축약 → memory `feedback_number_display` 위반

## 변경 사항

### GAS — `apps-script-v2/MobileAPI.js`

함수명 유지 `newMobileGetMonthlyRealized`. 응답을 **행 단위 14필드**로 변경.

```json
{ "success": true, "entries": [{
  "date":"2026-05-20", "month":"2026-05",
  "code":"005930", "name":"삼성전자", "category":"한국주식",
  "broker":"미래에셋", "account":"종합_랩",
  "quantity":15, "sellPrice":75000, "sellAmount":1125000,
  "avgBuyPrice":60000, "buyCost":900000,
  "fee":1500, "profit":223500, "returnPct":24.8
}, ...] }
```

- 정렬: 매도일 내림차순
- KPI/월별 집계는 클라이언트에서 derive (`useMemo`)

### 데스크 — `web-desk/src/api/gasApi.ts`

`MonthlyRealizedItem` 14필드로 확장 (date·broker·account·sellPrice·avgBuyPrice·sellAmount·buyCost·fee·category 추가).

### 데스크 — `web-desk/src/components/activity/ActivityPage.tsx`

**KPI 5칸 → 6칸** (Total Fees 추가):
- Realized P&L · Win Trades · Loss Trades · Win Rate · Avg Trade · **Total Fees**
- Total Fees: 전체 수수료 합 + per-trade 평균 (sub 라인)

**테이블 컬럼 재구성** (6컬럼 → 9컬럼):
- Date · 종목(이름 메인+코드 보조) · 증권사·계좌 · 수량 · 매수가 · 매도가 · 수수료 · 손익 · 수익률
- min-width 680px → 960px

**표시 규칙 적용**:
- 종목명 `text-amber font-medium` (메인), 종목코드 `text-xxs text-ink-faint` (보조)
- 막대 라벨 `+45만` → `+₩450,000` 풀 (`fmtSignedKrw` helper)
- 모든 금액 `Math.round(n).toLocaleString()`

**Sample data 8건 새 스키마로 갱신** (broker·account·매수가·매도가·수수료 채워 fallback도 실제와 같은 모양).

## 검증

### 자동 통과 ✓

- `apps-script-v2/push_safe.py`: 10개 .js 문법 검사 통과 + Secret.js 보호 정상 + push 성공
- `npx tsc --noEmit` (web-desk): 통과 (출력 없음)
- `npm run build`: success — `dist/assets/index-CrPVDNCO.js` 683kB

### Walk-through

실현손익 시트 행 입력 → 응답 → 화면 trace:

```
시트 행: [2026-05-20, 005930, 삼성전자, 한국주식, 미래에셋, 종합_랩,
         15, 75000, 1125000, 60000, 900000, 1500, 223500, 24.8]
  ↓ newMobileGetMonthlyRealized
응답 entries[i]: {date:"2026-05-20", month:"2026-05", code:"005930",
                 name:"삼성전자", broker:"미래에셋", account:"종합_랩",
                 quantity:15, sellPrice:75000, avgBuyPrice:60000,
                 fee:1500, profit:223500, returnPct:24.8, ...}
  ↓ DataProvider.refreshRealized → setRState({entries: res.entries})
  ↓ useRealized() → ActivityPage
KPI:
  Realized P&L: stats.total = +₩223,500 (또는 다른 행 누적)
  Total Fees:   stats.totalFee = ₩1,500 (1건 기준)
테이블 행:
  Date: 2026-05-20
  종목: 삼성전자 (amber) / 005930 (faint)
  증권사·계좌: 미래에셋 / 종합_랩
  수량: 15, 매수가: 60,000, 매도가: 75,000, 수수료: 1,500
  손익: +₩223,500 (text-gain), 수익률: +24.80%
```

### 🔴 사용자 확인 필요

**어디**: <https://ejunwon-lab.github.io/FD5to6/desk/> (GH Actions 배포 propagation 후, hard refresh)
**무엇**: 좌측 메뉴 **Trade Log** (또는 Activity)
**어떻게**: 새 산출물 `index-CrPVDNCO.js` 로딩 확인 (Network 탭) → 페이지 진입
**예상값**:
1. KPI 6칸 표시 — **Total Fees** 신규 칸 우측 끝
2. Monthly P&L 막대 라벨이 `+₩450,000` 같은 풀 숫자 (`+45만` 축약 X)
3. 표 첫 행이 **종목명(amber)** 메인 + 코드(작고 흐림) 보조
4. 표 컬럼 9개: Date · 종목 · 증권사·계좌 · 수량 · 매수가 · 매도가 · 수수료 · 손익 · 수익률
5. 정렬: 매도일 내림차순 (가장 최근 매도가 맨 위)
6. **실제 매매 데이터가 표시되는지** (sample 데이터가 아니라 본인 매매 — 증권사·계좌가 실제 본인 것과 일치하면 성공)

만약 여전히 sample (NVIDIA 1,900,000원 같은 합성 숫자)이면 응답 키 매칭 문제 — 콘솔에서 `gasApi.getMonthlyRealized` 응답 확인 필요.

## 후속 작업 (같은 날)

### A. 사이드바 메뉴 중복 제거 (commit `91a8402`)

**발견**: `App.tsx` 에서 `tradelog` 와 `realized` 두 NavKey 가 동일하게 `<ActivityPage />` 를 라우팅 → 사이드바 Workspace > Trade Log (F5) 와 Data > Realized P&L (F7) 가 *완전히 같은 화면*. 사용자 혼란 유발.

**변경**:
- `Sidebar.tsx`: `NavKey` 유니온에서 `'realized'` 제거 · Data 섹션에서 Realized P&L 항목 제거
- `App.tsx`: `case 'realized':` 줄 제거

**선택 근거**: Workspace = 분석/회고 화면들 (Dashboard·Holdings·Analysis·Indicators·Trade Log) 가 자연스러운 그룹. Data 섹션은 raw 조회/내보내기용 placeholder 모음 (Price History·Dividends 모두 PlaceholderPage) — Realized P&L 만 진짜 페이지로 떨어진 게 잔재. 정리 5분. 다른 옵션(`tradelog` 삭제 / 분리해서 다르게 구성)도 검토했으나 작업량 대비 가치 적음.

**검증**: tsc 통과 · `'realized'` 참조 grep 0건 (dead code 없음) · build `index-C3djgLZ0.js` · GH Actions success · production hash 일치.

### B. 사이드바 단축키 F1~F9 → 첫글자 (commit `b0166ae`)

**발견**: 기존 사이드바 hint (`F1`~`F9`, `●`) 는 *시각 라벨만* 있고 실제 keyboard 이벤트 핸들러 없었음. 사용자 요청은 "누르는 걸로 바꾸자" → 라벨 변경 + 실제 단축키 동작 신규 구현.

**키 매핑** (모두 unique):

| 메뉴 | 키 | 메뉴 | 키 |
|---|---|---|---|
| Dashboard | D | Price History | P |
| Holdings | H | Dividends | **V** (D 충돌) |
| Analysis | A | KIS Status | K |
| Indicators | I | Settings | S |
| Trade Log | T | | |

**구현** (`App.tsx`):
- `useEffect` 로 `window.keydown` 리스너 등록 (cleanup 포함)
- modifier 가드: `e.ctrlKey || e.metaKey || e.altKey` 면 무시 → 브라우저 단축키 (Cmd+D 북마크, Ctrl+S 저장 등) 보호
- 포커스 가드: `tagName` 이 `INPUT/TEXTAREA/SELECT` 또는 `isContentEditable` 면 무시 → 폼 입력 차단 없음
- `e.key.toLowerCase()` → 대소문자 무관 (Shift+D 도 작동)
- 매칭 시 `e.preventDefault()` + `setActive(nav)`

**검증**: tsc 통과 · build `index-DBtdR1kk.js` · GH Actions success · production hash 일치.

> 참고: Footer 의 F1~F5 라벨 (HELP·QUOTE·NEWS·CHART·REFRESH) 은 Bloomberg 풍 시각 장식이고 사용자가 언급 안 함 → 그대로 둠.

## 다음 작업 후보

- 사용자 검증 결과 받아 Trade Log 보완 (정렬 토글, 종목/증권사 필터, CSV 다운로드 등)
- Phase B 진입 (52주 위치·수익률 히스토그램 등)
- 대시보드 우측 상단 버튼 정리 (pending)
- Footer F1~F5 라벨 정리 (사용자 결정 시) — 사이드바 단축키와 일관성
