# 2026-06-10 — 텔레그램 푸시 안정화 + KR 리포트 B+C 개편

## 1. 텔레그램 푸시 (F 구조) 실측·수정
- **6/10 시작 드롭 발견**: telegram-push cron이 종일 미발화. 원인 = 시작 틱 4개가 2시각(09:00·09:03, 12:30·12:33)에 몰려 GitHub 아침 윈도우 통째 드롭에 전멸. market-report는 별 cron이라 발화(09:28 US 요약). → **수정**: cron `0,20,40 0-6 * * 1-5`(20분 간격 전 시간대 spread) + 루프 120분(6틱분 겹침). 당일은 수동 dispatch로 복구. (errors.md 2026-06-10)
- **손익 +0원 버그 수정**: 푸시 합계가 +0/직전값 → `tgPushPnL`이 부분 갱신(가격+보유)만 해 **합계 손익 출처인 추이기록 AD2(`logToTrendSheet`)·환율이 stale**였음. 오늘손익은 `computeStockMetrics`(updatePositionFromLedger 끝)가 돌아 정상이었음. **수정**: `tgPushPnL`·`tgRefreshAndPush` → `updateAllNew()` 전체 갱신. **v11 배포**(push_safe + API로 버전 고정 배포 v10→v11). ㄱㄱ 라이브 검증 OK. (errors.md 2026-06-10)
- **잔여**: 6/11 아침 spread cron 시작 적중 모니터링. 또 드롭이면 Cloudflare Worker cron(인증 1회 필요).

## 2. 전역 환경 동기화
- `~/.claude/settings.json`(→ harness `global-settings.json` 심링크)의 `ask`에서 **인터프리터/dev 실행(python·node·npx 등 20개) 제거** → 무프롬프트. 위험(rm·sudo·설치·ssh·git파괴)은 ask/deny 유지.
- harness repo(claude-2026) 커밋·푸시 → 모든 컴퓨터 동일 환경. (memory: [[feedback_global_env_consistent]])
- 교훈: `git push 2>&1 | tail`은 push 실패를 가림(tail exit code). origin 직접 grep으로 반영 검증. (errors.md 2026-06-10)

## 3. KR 리포트 B+C 개편 (로테이션 렌즈 + 기회 파이프라인)
사용자 리프레이밍: 단순 시장 리캡 → **내 의사결정 지원 + 미래 기회 발굴**. 5안→블렌드(1~4 전부)→B+C(자금흐름 렌즈 + 기회 파이프라인) 채택.
- **Phase 1 (배포·검증 완료)**: `kr-prompt.md` 재작성 — ⚡한줄 · 🔄자금흐름(OUT/IN/판정) · 🛡️내 포트 수비(수급+논리점검+관찰) · ⚠️리스크 레이더(정성 집중도) · 🎯기회 파이프라인(①신규②관찰③연구) · 💬종합-다음수. + `kr-theses.md`(종목별 논리 초안 — **사용자 확정 대상**) + `_kr_pipeline.md`(다일 파이프라인 상태, 에이전트 Read/Write·커밋 누적).
  - **출처 가드레일**(최상단 절대 규칙): 목표주가·거시지표 등 미수집 수치 숫자 금지. dry_run 재검증 → 환각 "427,917원"·"CPI 4.2%" 차단 확인.
  - dry_run 2회로 B+C 형식·thesis 적용·파이프라인 첫날 처리·가드레일 검증 완료.
- **Phase 2 (진행 중)**: 설계노트 `docs/plans/2026-06-10-kr리포트-phase2.md`.
  - 2b 기회 자동선별: Naver 섹터 구성종목 엔드포인트 **러너 진단 통과**(HTTP 200, IP차단 아님) → 실현 가능. 임시 `_kr_diag` 제거.
  - 2a 지표(익스포저%·MDD): **미구현**. 설계 확정 — GAS `getPortfolioMetrics`(상대%·MDD만, 원화 0) + doPost 분기 → 배포 → market-report.yml KR step **env로 GAS URL·시크릿 주입**(public 프롬프트에 안 박음, 에이전트 Bash로 $env curl) → 프롬프트 연동 → dry_run(원화 미노출·환각 0 게이트). MDD = 추이기록 일별 Q열(총자산, row5↓) peak-to-trough.

## 다음 세션 진입점
- Phase 2a 구현(위 순서) → dry_run 검증. 그 후 6/11 KR 리포트가 첫 실전 B+C.
- 사용자: `kr-theses.md` 논리 초안 검토.
- 텔레그램: 6/11 아침 spread cron 시작 적중 확인.
