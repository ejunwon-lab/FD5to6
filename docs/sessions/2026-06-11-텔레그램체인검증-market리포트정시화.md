# 2026-06-11 — 텔레그램 daisy-chain 라이브 검증 + market-report 정시화 구현

## 1. 텔레그램 push daisy-chain — 6/11 라이브 검증 통과·완료 ✅
6/10 밤 전환한 daisy-chain을 정상상태 첫 거래일에 로그로 검증, 3항목 전부 PASS.

- **① 첫 푸시 + 간격**: 09:05 KST 첫 발화(run 27310688349, `T00:05 UTC`), 다음 09:26 — ~20분 간격.
- **② result=sent (09:00~16:00) = 총 20회** (22슬롯 중, 나머지 skip-dedup/skip-lock 정상). 16:00 이후 `skip-offhours` 정확 전환.
  | run | KST | sent |
  |---|---|---|
  | 27310688349 | 09:05~09:31 | 2 |
  | 27315551272 | 09:31~11:30 | 5 |
  | 27319837866 | 11:30~13:29 | 6 |
  | 27323857240 | 13:29~15:28 | 5 |
  | 27328357385 | 15:28~16:00 | 2 |
- **③ 체인 연속성**: self-dispatch가 22:33(6/10)→00:31→02:30→04:29→06:28→08:26(6/11) **~2h 무중단**(전부 workflow_dispatch=bot). cron run은 concurrency로 cancelled/pending 강등 = "정상상태 best-effort 의존 0회" 실증.

→ pending.md에서 daisy-chain 항목 제거(완료). 설계 원본: `docs/plans/2026-06-10-텔레그램-daisy-chain.md`.

## 2. market-report 정시화 — B+ 구현 (라이브 관찰 남음)
KR 리포트가 타깃 17:02 KST 대비 +3.3~3.8h 지연(6/9·10·11 연속, GH schedule best-effort). `/design-check` 게이트 통과 후 코딩.

**설계 노트**: `docs/plans/2026-06-11-market-report-정시화-daisychain.md` (4항목 + A/B/C 선택지, 사용자 B+ 채택).

**메커니즘 (B+)**: 검증된 telegram 24/7 체인이 리포트 창(KST 08:0x US / 17:0x KR) 통과를 감지 → `gh workflow run market-report.yml -f type=<t> -f auto=true` 이벤트 구동 dispatch(cron 지연과 무관). market-report cron 4개는 재시드 백업으로 강등.

**멱등 3중** (반복 dispatch 레이스 = errors.md:32 "2회 안전망 race" 동류 회피):
1. telegram run 내 `/tmp/mr_<t>_<date>` 메모 — 같은 run 재dispatch 차단.
2. `gh api contents` 존재확인 — 이미 커밋된 리포트면 dispatch 안 함.
3. market-report job `concurrency: market-report-{us,kr}` + `auto=true` 가드 — 중복 dispatch는 1 running+1 pending 수렴, pending은 커밋된 파일 보고 skip.

**변경 파일**:
- `.github/workflows/market-report.yml`: workflow_dispatch `auto` 입력, 가드 `schedule`+`auto=true` 확장, job별 concurrency, 헤더 코멘트(cron 백업 강등).
- `.github/workflows/telegram-push.yml`: `permissions.contents: read` 추가, poke 스텝 `GH_TOKEN`/`GITHUB_REPOSITORY` env, poke 루프 안 `check_report_windows`/`maybe_dispatch_report`(`|| true` 격리).

**로컬 검증 (자동 통과)**:
- YAML 파싱(ruby) 2파일 ✓ · bash -n 전 run 블록 ✓.
- 창 판정 walk-through: 0802→US, 1702→KR, 0801/0900/1701 경계 정확, 토1702 주말 skip. **octal 파싱 버그(`08xx`)를 `$((10#$hm))`로 회피** — 설계 단계에서 선제 차단.
- auto 가드 walk-through: `auto=true & file=yes → SKIP`(멱등 핵심) 등 5케이스 정확.

**🔴 남은 것**: 다음 거래일(6/12 금) 라이브 실측 — KR 리포트 17:0x~17:1x KST 발화·도착(현 ~20:4x), market-report run 1개 success + 나머지 concurrency 흡수(레이스 0, 특히 1회성). 실패 시에만 A(독립 체인) 재검토.

## 비고
- 오늘 17시대 KR 첫 런(B+C 개편 첫 실스케줄)은 **이 push 이전 옛 cron** 적용이라 ~20:4x KST 지연 도착 예상 — KR B+C 파이프라인 시드 누적은 그때 시작. 별도 pending 유지.
