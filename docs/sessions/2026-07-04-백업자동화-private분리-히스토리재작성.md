# 2026-07-04 — 푸시 +0원 · 모바일 개선 · 백업 자동화 · private 분리 · 히스토리 재작성

## 1. 텔레그램 푸시 "오늘 +0원" (GAS v25)
- 원인: KIS 일시 전면 실패 시 carry-forward(전일가 복제) → 전 종목 당일 0인데 푸시에 단서 없음. errors.md 2026-07-04 기록.
- 해결: `kis_carried_status` 기록 + 푸시에 "⚠️ KIS 시세 조회 전체 실패 — 직전가 기준" 라인. 정상 시 메시지 불변. 다음 발생 시 ⚠️ 동반으로 확인.

## 2. web-desk 모바일 사용성 11건 + TOP 상승/하락
- 핵심: 보유 카드 우측 수식 블록이 종목명을 밀어냄 → flex-wrap+이름 최소폭으로 줄바꿈(카드 2종). 그 외 Allocation 1열, 심볼/마켓 모바일 숨김, 히트맵 2열, 52주 이름칸 유동, 터치 타겟 확대, 단축키 힌트·Footer 범례 숨김, TopBar Asia/Seoul 고정.
- 오늘 TOP 상승/하락 2칸 분리(change 정렬 시 6열) — 사용자 요청.
- 장기: 모바일 컴팩트 모드 pending 등재.

## 3. 시트 백업 로컬 자동화 (사용자 결정: repo ✗·Drive ✗·로컬 ✓)
- GAS `action=backupData`(v26, secret 게이트) → `scripts/backup_sheets.py`(재시도 3회 — GAS 대용량 응답 간헐 오류 흡수) → `backups/`(gitignore) 시트별 CSV+원본 JSON, 최근 20회 보관.
- launchd 주 1회(일 21:00) + `scripts/setup_backup_launchd.sh`(맥마다 1회, 경로·python 자동 감지, 전제조건 점검). kickstart 실검증.

## 4. 리포트 private repo 분리 (`ejunwon-lab/FD5to6-reports`)
- 배경: main repo PUBLIC(GH Pages) — 리포트에 보유·비중·논리 노출.
- 구조: 워크플로가 private repo를 `docs/reports` 경로에 checkout(deploy key `REPORTS_DEPLOY_KEY`) → 기존 경로 참조 무수정, 커밋만 private로. telegram-push의 contents 존재확인은 제거(가드 멱등으로 충분). 로컬 docs/reports = clone(run.sh pull).
- **프롬프트 3종+kr-theses도 `_config/`로 이전** (보유 종목 목록·논리 비공개화) — 워크플로 cat 경로·프롬프트 내부 Read 경로 전환. 편집은 이제 `docs/reports/_config/`에서.
- dry_run 2회 통과(28702135651·28703070632). 라이브 커밋 검증은 7/5 US·주간.

## 5. 히스토리 재작성 (사용자 승인)
- git-filter-repo로 `docs/reports/`+프롬프트 3종+kr-theses를 전 히스토리 purge → force push(fa3e5a0). fresh clone 검증: 대상 경로 커밋 0건·히스토리 종목명 검색 0건·242커밋. 재작성 후 Actions 생존(diag success).
- **run.sh 자기복구 추가**: 갈라진 히스토리 감지 → backup-* 브랜치 보존 후 `checkout -B main origin/main` — **다른 맥 폴더 삭제 불필요.** 이 맥 회귀 검증 통과.
- 잔여(pending): 다른 맥 3단계(실행@·`.env`·launchd 스크립트), GitHub Support GC 요청(선택), 세션 문서 등 비리포트 문서의 종목명 단편은 범위 밖으로 수용(설계 노트 명시).
- 주의: run.sh의 memory 복원이 라이브 memory를 repo 미러로 덮음 → 세션 중 memory 수정은 즉시 미러 커밋 필요(이번에 1회 발생·복구).

## 설계 노트
- `2026-07-04-푸시-0원-stale단서.md` · `2026-07-04-시트백업-로컬.md` · `2026-07-04-리포트-private-분리.md` · `2026-07-04-히스토리-재작성.md`
