# 2026-05-06 뉴 시스템 — 실현손익 + 대시보드

## 작업 내용

### 1. *실현손익* 시트 추가 (NewSystem.js)
- `_setupRealizedPnLSheet()` 함수 추가
- `setupNewSystem()`에 5번째 시트로 편입
- 컬럼: 매도일 / 종목코드 / 종목명 / 분류 / 증권사 / 계좌 / 매도수량 / 매도단가 / 매도금액 / 평균매입단가 / 매입원가 / 수수료 / 실현손익 / 수익률(%)
- `updatePositionFromLedger()` 실행 시 원장 재생하며 매도 건 자동 계산 → *실현손익* 시트 갱신

### 2. 과거 거래 이력 임포트 (NewSystem.js)
- `importHistoricalTrades()` 함수 추가
- `매입매수정보.xlsx` 파싱 → 80건 (매수 40 + 매도 40)
- 기존 원장 데이터와 합산 후 **날짜순 정렬** 재작성
- 변환 처리: 한국어 날짜 파싱, 종목코드 앞 0 제거, 증권사 오타 수정, 계좌명 정규화

### 3. 가격 히스토리 버그 수정 (NewSystem.js)
- `_normCode()` 헬퍼 추가: 순수 숫자 코드 앞 0 제거 (005930→5930), 혼합 코드 유지 (0047A0)
- `updateNewPriceHistory()`: 코드 정규화로 중복 열 추가 버그 + 값 못 가져오는 버그 수정
- 날짜 형식 `yyyy-MM-dd` → `yyyy-MM-dd HH:mm:ss`로 변경 (upsert는 날짜 prefix로 판단)
- `_getLatestPrices()`, `updatePositionFromLedger()` 에서도 `_normCode()` 적용

### 4. 대시보드 (Dashboard.js 신규)
- `buildDashboard()` 함수: *대시보드* 시트 생성/갱신
- 6개 섹션:
  1. 요약 카드 (총매입 / 총평가 / 운용손익 / 확정수익 / 총수익 / 승률)
  2. 계좌별 현황
  3. 분류별 현황
  4. 보유 종목 현황 (수익률 순)
  5. 확정 수익 월별 요약
  6. 수익 Top 5 / 손실 Top 5
- `updatePositionFromLedger()` 완료 시 자동 호출

### 5. 메뉴 추가 (Main.js)
- `onOpen()`에 `🗄️ 새 시스템` 메뉴 추가 (시스템 관리 우측)
- 항목: 시트 초기화 / 과거 이력 불러오기 / 포지션 재계산+대시보드 / 대시보드만 / 가격히스토리 / 종목히스토리

## 설계 결정
- 종목 식별: 매수번호(Lot ID) 대신 **메모 컬럼** 활용 (평균단가법 유지, 증권사 기준과 일치)
- 코드 정규화: 구글 시트가 숫자형 코드를 자동으로 앞 0 제거하므로 _normCode()로 일관 처리

## 미완료
- GAS 에디터에서 순서대로 실행 필요:
  1. `setupNewSystem` — 시트 5개 생성
  2. `importHistoricalTrades` — 과거 이력 80건 추가
  3. `updatePositionFromLedger` — 포지션 + 실현손익 + 대시보드 생성
- Phase 3: iOS `mobileGetPortfolio` → *포지션* 기반 전환 (미착수)
