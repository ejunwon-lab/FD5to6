# 2026-05-12 추이 기록 데이터 소스 변경 + Claude 권한 설정

## 완료된 작업

### 1. `_updateNewTrend()` 데이터 소스 변경 (`NewSystem.js`)

**opNow / opBuy 소스 변경:**
- 이전: `*종목상태*` 현재가 × 수량 (+ `_getStockStatusMap()` 호출)
- 변경: `*보유현황*` 합계행(`r[0] === '합계'`)에서 직접 읽기
  - `opBuy` = 합계행 r[8] (매입금액)
  - `opNow` = 합계행 r[10] (평가금액)
- `_getStockStatusMap()` 호출 제거

**호출 위치 변경:**
- 이전: `updateNewCurrentPrice()` 안에서 호출 (보유현황 갱신 전)
- 변경: `updatePositionFromLedger()` 마지막에 `SpreadsheetApp.flush()` 후 호출
  - 보유현황이 최신 상태로 갱신된 뒤 추이 기록

### 2. Section C row 2 스냅샷 규칙 변경 (`NewSystem.js`)

**새 규칙:**
- `U2`: 현재 타임스탬프
- `V2:AD2`: 마지막 데이터 행(rows 5+)의 V~AD 값 — 항상 마지막 실제 데이터 반영
- `AE2:AF2`: 기록 안 함 (AH/AI 캐시에서 관리)

**prevTotProfit 계산 변경:**
- 이전: `writeRow - 1` 행의 AD 직접 읽기 (복잡한 row 탐색)
- 변경: row 2의 AD(index 9) 에서 읽기 — row 2가 항상 최신 스냅샷이므로 단순화

**AJ/AK 백업 소스 변경:**
- 이전: row 2의 AE/AF (index 10/11) → 더 이상 기록 안 함
- 변경: row 2의 AH/AI (index 13/14) → 이전 거래일 diff 캐시 백업

**row 2 읽기 범위 확장:**
- 이전: 12열 (U~AF)
- 변경: 17열 (U~AK) — AH/AI 포함

### 3. Claude Code 전역 권한 설정 (`~/.claude/settings.json`)

**자동 허용 추가 (모든 프로젝트 적용):**
- `Bash(*)` — 모든 셸 명령
- `Read(**)` — 모든 파일 읽기
- `Edit(**)` — 모든 파일 수정
- `Write(**)` — 모든 파일 생성
- `WebFetch(*)` / `WebSearch(*)` — 외부 접근

**자동 거부 (파괴적 명령 차단):**
- `rm -rf *`
- `git push --force*` / `git push -f *`
- `git reset --hard*`
- `git branch -D *`
- `* --no-verify*`

## 배포
- `python3 apps-script/push_safe.py new` → 7개 파일 배포 완료 (2회)

## 미완료 / 확인 필요
- `updatePositionFromLedger` 실행 후 `*추이 기록*` row 2 V~AD 실제 기록 확인 필요
- 신시스템과 구시스템 병행 운용 중 (신시스템 안정화 진행 중)
