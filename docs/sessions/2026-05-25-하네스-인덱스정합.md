# 세션: 2026-05-25 — 하네스 강화 (architecture.md + 인덱스 정합 자동 검증)

전 세션(`2026-05-25-phase-a-자동캐시-규칙.md`)에서 Phase A·자동 캐시·표시 규칙을 마친 후, 사용자가 "전체 시스템 한 곳 정리"와 "인덱스 자동 갱신·stale 감지"를 차례로 지시. AI 하네스 메타 보강 세션.

## 진행 흐름

1. **시스템 종합 문서**: 사용자가 "GAS·iOS·웹·텔레그램·데스크 전체를 한 마크다운에" 요청 → 기존 `architecture.md`(2026-04-25 이래 방치) 전면 재작성. 5 컴포넌트·데이터 흐름 4 경로·시트 스키마·자동 트리거 표·공통 규칙·작업 진입점 매핑
2. **CLAUDE.md 압축 요약**: architecture.md가 자동 로드 안 됨 → 핵심 요약(5 컴포넌트·트리거 표·표시 규칙·작업 진입점) 39줄 압축해 CLAUDE.md "## 시스템 한눈에" 섹션 신설 (매 세션 자동 컨텍스트)
3. **자동 갱신 규칙 영구화**: 사용자 "니가 자동으로 늘 업데이트 하도록 기억해" → memory `feedback_index_docs_auto_update` 신규. 변경 유형별 자동 갱신 매핑 표 박음
4. **하네스 메타 논의**: 사용자가 "이게 하네스라 볼 수 있나" → 6 구성요소 매핑 보고. 약점 = stale 감지 자동화 없음 → 사용자 "보강해봐"
5. **Stale 감지 인프라 3 레이어 구축**:
   - `scripts/check_stale.sh` 신규 (전수 정합 검사)
   - `scripts/save.sh`에 staging hint 통합 (commit 직전 자동 경고)
   - CLAUDE.md "## 인덱스 정합 검증" 섹션 + memory 도구 안내 추가

## 코드 변경 (commit 기준)

| 커밋 | 내용 |
|---|---|
| `2d721d1` | `docs/architecture.md` 전면 재작성 (65→290줄) + CLAUDE.md 코드 탐색 진입점 추가 |
| `932bff2` | CLAUDE.md "## 시스템 한눈에" 섹션 신설 (39줄) |
| `fbae1e2` | memory `feedback_index_docs_auto_update.md` 신규 + MEMORY.md 인덱스 |
| `2ad71e0` | `scripts/check_stale.sh` 신규 + `save.sh` staging hint + CLAUDE.md "인덱스 정합 검증" 섹션 + code-map.md Telegram 운영 함수 8개 보강 |

## 주요 결정

- **architecture.md를 명시 retrieval, CLAUDE.md 압축본을 자동 컨텍스트**로 역할 분리. 깊이는 architecture.md, 매 turn 자동 적용은 CLAUDE.md
- **인덱스 자동 갱신은 Claude 책임**: 변경 유형별 매핑(GAS 함수→code-map, 시트 컬럼→architecture, API 필드→api-reference, 트리거→architecture+features+CLAUDE.md, 설계 결정→decisions, 새 기능→features, 영구 규칙→memory)
- **Stale 감지는 차단형이 아닌 hint형**: save.sh가 patterns 매칭으로 경고만 출력, 차단 안 함. Claude가 판단해서 같은 turn에 추가 갱신
- **check_stale 검사 범위 제한** (false positive 최소화): "코드에 있는데 인덱스에 없는" 케이스만 검사. 반대 방향(인덱스에 있는데 코드 없음)은 alias·라이브러리 등 false positive 많아 제외
- **last updated 30일+ 경과는 경고**: 자동 갱신은 안 함 (Claude가 손으로)

## 첫 실행에서 잡은 실제 stale

`scripts/check_stale.sh` 첫 가동 결과:

- **`menuUpdateAll`** — `updateAllNew`의 alias, code-map 미등재 → 등재 (한 줄)
- **Telegram 운영 함수 8개** 미등재:
  - `tgAddChatId`·`tgRemoveChatId`·`tgListChatIds` (chat_id 화이트리스트 관리)
  - `tgSetBotToken`·`tgSetWorkerUrl`·`tgClearWorkerUrl` (Properties 설정)
  - `tgShowSecret` (webhook secret 확인)
  → code-map.md Telegram.js 섹션에 "Properties 관리" 하위 섹션 신설하여 8개 등재

검사 후 재실행: `✓ 인덱스 정합 — stale 의심 없음`

## 메모리 갱신

이번 세션 신규 1건:
- `feedback_index_docs_auto_update.md` — 인덱스 문서 자동 갱신 규칙 + check_stale·save.sh 활용법

## 하네스 구성요소 매핑 (이번 세션으로 완성)

| 하네스 구성요소 | 이 프로젝트 구현 |
|---|---|
| 자동 컨텍스트 | CLAUDE.md "시스템 한눈에" + 글로벌 CLAUDE.md |
| 영구 메모리 | `memory/*.md` + MEMORY.md 인덱스 (자동 로드) |
| 검색·진입점 인덱스 | `architecture.md` (명시) / `code-map.md` / `api-reference.md` / `errors.md` |
| 검증 게이트 | CLAUDE.md "주장 검증·변경 검증" 절차 + walk-through 의무 |
| 결정적 자동화 | `save.sh`·`run.sh`·`push_safe.py` |
| 상태 추적 | TodoWrite + `pending.md` + `sessions/` |
| 행동 조건화 | feedback memory들 (숫자 풀·종목명 메인·인덱스 자동 갱신 등) |
| **Stale 감지** (이번 세션 보강) | `check_stale.sh` 전수 + `save.sh` staging hint + memory 활용 안내 |

## 한계 (정직)

- check_stale false positive: alias·외부 라이브러리 이름 등 → 의도면 무시
- last updated 자동 갱신은 없음 (Claude가 손으로 today's date 적용)
- 시트 컬럼·트리거 시각·iOS 변경은 코드에서 자동 추출 어려워 Claude 자가 점검에 의존
- check_stale를 매 작업 자동 실행하지는 않음 — Claude가 작업 마무리 시점에 판단해 실행

## 다음 작업 후보

- Phase B 진입 (`desk-enhancement-plan.md` Phase B): GAS 일부 확장 + 52주 위치 막대·수익률 히스토그램·벤치마크 outperformance·월별 히트맵·세금 시뮬
- 또는 사용자 피드백 보강 (Account P&L 정렬·필터 등)
- 또는 `pending.md`의 미해결 항목 (대시보드 우측 상단 버튼 정리·iOS 공휴일 버그 검증 등)
