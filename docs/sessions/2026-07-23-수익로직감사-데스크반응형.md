# 2026-07-23 — 수익 로직 전수 감사·수정 + 데스크 모바일 반응형

## 1. 기간별 수익 라벨 + 계산 로직 확인
- 웹 "기간별 번 돈" → **"기간별 수익"** 라벨 변경 (DashboardPage.tsx)
- 계산 확인: *추이 기록* AD(합계수익=확정+운용, Trend.js) 양끝 diff — **실현이익 이미 포함** (웹 `computePeriodProfits`·데스크 equityCurve 동일 원천)

## 2. 수익 계산 전수 감사 → 4건 수정 (설계: `docs/plans/2026-07-23-수익로직-수정.md`)
- **A. 히스토리 윈도**: `slice(-180)` → "최소 180거래일 + 전년 12/1까지 보장" (MobileAPI `newMobileGetProfitHistory`) — 10월경 웹 "올해" 타일이 조용히 변질될 잠복 결함 제거
- **B. 합계 수익률 분모**: totalBuy → 누적 투입원가(totalBuy+매도분 매수원가) — 매도 회수 원금 누락 과대 교정. **4클라이언트 % 하락이 정상**
- **C. 데스크 Monthly Heatmap**: 윈도 첫 달 prev=0 부풀림(누적 전체가 그 달 수익으로 표시, 색 스케일·YTD 오염) → 첫 달 baseline 전용 제외
- **D. 데스크 EquityChart**: `slice(-N)`(거래일 N개=라벨보다 긴 기간) → 날짜 산술 필터
- 배포: push_safe + `gas_redeploy.py` 웹앱 3배포 **v33** (web·desk·iOS는 devMode HEAD라 push만으로 반영)
- 검증: web vitest 54/54·tsc / desk tsc·build / walk-through 4건 (errors.md 2026-07-23 기록)

## 3. 데스크 모바일 반응형 — Dashboard 1단계 완료
- 결정: 별도 모바일 페이지 대신 **데스크 반응형** (3번째 클라이언트 금지, DataProvider 재사용)
- **MobileTabBar 신설** — 모바일(<lg) 하단 탭 6개(Dash·Today·Hold·Anlys·Indic·Trade), safe-area 패딩, Footer는 lg+만. 드로어는 Data/System 메뉴용으로 유지
- **App 셸**: `h-screen`→`h-[100dvh]`(iOS Safari 주소창 대응), viewport `viewport-fit=cover`
- **홀딩스 접기**: 기본 상위 9종목 + "전체 N종목 보기" 토글 (전 뷰 공통, 합계·현황 strip은 전체 기준 유지) — 데스크톱도 스크롤 단축
- **필터바 모바일 2행화**: 정렬 chips 가로 스크롤 / 뷰 토글+검색(flex-1) 분리
- 부수 수정: Holdings 패널 meta `₩x.xxM` 축약 → 풀 숫자 (feedback_number_display 위반이었음)
- 검증: desk tsc·vite build·vitest 8/8
- 다음 단계: Today → Analysis 순 (pending 참조)

## 4. 웹 배포 7일 침묵 실패 발견·복구
- 데스크 배포 확인 중 발견: `Deploy Web to GitHub Pages`가 7/16부터 전 실행 failure (웹앱 라이브가 7/16 상태로 정지)
- 원인·2단계 해결·교훈은 errors.md 2026-07-23 항목 — 최종: lock을 CI와 같은 npm@10으로 재생성, npm 10·11 양쪽 ci 통과
- 라이브 번들 검증: 웹 "기간별 수익" 문자열·데스크 탭바/접기 문자열 서빙 확인

## 5. 홀딩스 카드 폴드 (A안 2단, 사용자 선택)
- Web/Terminal 카드: 접힘(헤더=종목명·현재가·당일등락만) ↔ 펼침(요약+상세 통합, 중복 3필드 제거)
- 기본: 데스크톱 펼침 / 모바일(<lg) 접힘 (사용자 선택). 필터바 "모두 접기/펼치기" 전역 토글 + 카드 클릭 개별 오버라이드
- 상위 9종목 접기와 독립·중첩 동작. Holdings 탭 자동 적용. List 뷰 무변경
- 피드백 반영: 접힘 헤더를 **정확히 2줄**(종목명|현재가▼ / 등락%·내 등락총액)로 컴팩트화 — 코드·기간·상세버튼·×수량 수식은 펼침 전용, 패딩 축소
- **사용자 아이폰 실기기 확인 완료** (하단 탭바·카드 폴드·2줄 헤더 — 첫 확인은 index.html max-age=600 캐시 때문에 지연, 탭 재접속으로 해소)

## 사용자 확인 대기
- 데스크 배포 후: Heatmap 첫 달 회색 처리 + 색 대비 회복 / all-time % 하락(금액 불변) / 웹 기간별 수익 값 불변
