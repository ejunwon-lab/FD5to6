# 2026-05-10 뉴시스템 독립화 + 대시보드 개선

## 작업 목표
- NewSystem.js를 구 트래커에서 완전 독립
- *종목상태* 시트 신설 (KIS API 직접 조회)
- 현재가/히스토리 업데이트 분리 (7:55 AM 트리거)
- 대시보드 13컬럼 확장 + 개선

## 주요 변경사항

### GAS 배포 구조 개선
- `apps-script/` → 구 시스템 (17개 파일)
- `apps-script-new/` → 신 시스템 (5개 파일: Code, Dashboard, KIS_API, Main, NewSystem)
- `push_safe.py new/old` 인수로 대상 선택
- KIS_API.js 수정 시 양쪽 모두 배포

### NewSystem.js — 구 트래커 의존성 완전 제거
- `getTrackerActiveData`, `CONFIG.*` 참조 0건
- `*종목상태*` 시트 신설 (11컬럼: 종목코드·현재단가·등락·등락률·52주고·52주저·1M~1Y·갱신시간)
- `updateNewCurrentPrice()`: 현재가·등락만 갱신 (장중 다회 호출용)
- `updateNewStockHistory()`: 히스토리+현재가 전체 갱신, M1에 타임스탬프 기록
- `updateNewPriceHistory()`: `updateNewCurrentPrice` wrapper (Main.js 호환)
- `updatePositionFromLedger()`: statusMap 우선, priceMap fallback, 23컬럼
- `updateFxRates()`: GOOGLEFINANCE로 USD/GBP 환율 갱신

### Dashboard.js — 13컬럼 확장
- 보유 종목 현황에 1M·3M·6M·1Y 추가
- 상단에 `1M~1Y 갱신: YYYY-MM-DD HH:mm` 표시 (*종목상태* M1에서 읽음)
- 텍스트 중앙 / 숫자 우측 정렬
- 계좌 정렬: 미래랩→미래IRP→삼성종합→삼성ISA→삼성IRP
- 계좌명 자동 축약: `퇴직연금_개인IRP` → `퇴직IRP`
- 1M~1Y: `+33.33%` / `-5.67%` 포맷

### Main.js — 트리거 분리
- `scheduledHistoryUpdate`: 7:55 AM (히스토리)
- `scheduledDailyUpdate`: 17:30 (현재가·보유현황·대시보드)
- `setupTriggers()` 한 번에 등록
- 메뉴에 `1M~1Y 히스토리 갱신 (수동)` 추가

## 버그 수정
- `000660` 등 앞자리 0 종목코드: `padStart(6,'0')` 처리
- `updateFxRates is not defined` 누락 함수 추가
- 신시스템 GAS에 구 시스템 파일 17개 잘못 배포 → 복구

## 다음 세션
- 분석 대시보드 신규 추가 (포트폴리오 대시보드 개선 완료 후)
- `setupTriggers` 실행으로 7:55/17:30 트리거 등록 확인 필요
