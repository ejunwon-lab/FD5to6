# 2026-06-20 — 시장 리포트 PB 격상 + 이메일 채널 + 정합성·신뢰 보강

매일 시장 리캡 → **내 포트 중심 PB 자문 리포트**로 격상 + **이메일 셀프발송 채널** 신설. 이어서 데이터 정합성·거시 환각·휴장 처리까지 보강. GAS v18→v22, 커밋 8.

## 1. PB 격상 (커밋 8c0e38b)
- kr/us-prompt **7섹션 역피라미드**: ⚡한줄+액션 → 💼내포트 → 🧭논리·방향 → 📊시장 → 🔮전망·시나리오 → 🎯기회 → 📅변수. 결론·내포트를 위로, 시장데이터는 근거로 아래. **종합 산문 폐지·한 사실 한 번**.
- 추가 콘텐츠 4: 논리추적·리밸런싱/현금신호·벤치대비·이벤트캘린더.
- 데이터 경계: 가격·수급·로테이션 + 조건부 시나리오. **밸류에이션 목표가 단정 금지**.
- US 리포트 핵심 = **미국장→내일 한국 반도체(48%) 선행 신호** 번역.

## 2. 이메일 셀프발송 채널 (GAS v18, 커밋 8c0e38b)
- `doPost(action=emailReport)` → `MailApp.sendEmail`. 수신=발송=`getEffectiveUser()` 소유자(**halcyon.public@gmail.com**, 쿼터 100 consumer). 주소 하드코딩 0.
- `appsscript.json`에 `script.send_mail` 스코프 추가 → 사용자 `_emailReportSelfTest` 1회 실행으로 권한 승인 완료(11:17 도착 확인).
- market-report.yml US/KR "Send Email" 스텝(jq 안전직렬화, continue-on-error) + US job GAS env 주입.
- send_telegram.py 4096 분할 안전망.

## 3. 이메일 안전장치 5중 (GAS v19/v20, 커밋 56ad8c5)
위협모델: 수신자 getEffectiveUser 고정이라 **제3자 발송 구조적 불가**. 남은 위험(본인 스팸폭주·임의내용·사고정지) 방어:
1. Kill switch — Properties `email_disabled=1` (`emailKillSwitch_ON/OFF` 토글함수)
2. 제목 화이트리스트 — `Market (Close|Wrap)` 패턴만
3. 일일 상한 6통 — subject 무관 카운터(dedup 우회 폭주 차단)
4. 응답 수신주소 미노출 (public 로그 보호)
5. (구조적) 수신자 고정

## 4. 데이터 정합성 (GAS v21, 커밋 c3d0a98)
- 문제: operatingRate(누적 운용수익률)는 매매 시 분모(매입원가) 변동으로 오염.
- 해결: `portfolioReturn{d5,d20}` = **일별 총자산 변화율(dRate) 복리누적**. dRate는 현금↔주식(매수/매도)엔 총자산 불변→0, 시장변동만 반영, 입출금만 왜곡. → KOSPI/KOSDAQ N일 변화율과 **단위 일치 비교**.
- 벤치: KOSPI+KOSDAQ (1-A Yahoo `^KS11`·`^KQ11` 5d). operatingRate는 누적수익률 수준 1개로 축소.
- 실증(dry_run): "5일 +7.29% vs KOSPI +5.93%·KOSDAQ -6.52% 초과".

## 5. 거시 환각 가드레일 (커밋 c3d0a98)
- us-prompt ⛔에 거시·정책·지정학 서사 **출처(기사) 강제 + 일반지식 메우기 금지 + 해석은 추론 라벨**.
- 실증: US 거시마다 "(한경)", "기사 미확인" 라벨.

## 6. 이메일 HTML 서식 (GAS v22, 커밋 c3d0a98)
- `_mdToHtml`: MarkdownV1→HTML(`*굵게*`→b, `_이탤릭_`→i, ```코드블록```→pre). 코드블록 안 esc로 표 안 깨짐. `htmlBody` + plain fallback. node 재현 검증.

## 7. 휴장 처리 A — 직전거래일 리뷰 (커밋 97b10bc, b8b1c34)
- 휴장이어도 "휴장 한 줄" 대신 **직전 거래일 마감 기준 풀 PB 리포트**(빈 메시지 폐지). GAS 포트지표는 휴장 무관 최신이라 💼 섹션 충실.
- KR 폴백 보강: Naver basic 휴장 모호 → Yahoo `^KS11`/`^KQ11`/`.KS` 우선순위 + 파일 생성 필수.
- 실증: US·KR 둘 다 found=yes, "직전거래일 2026-06-19(목) 기준" 풀 리포트.

## 검증
- dry_run(both, 27859940380 + KR 27860142732): portfolioReturn·벤치비교·거시출처·원화0·휴장폴백·자수≤4096 전부 통과.
- node: portfolioReturn 복리누적(매매강건), _mdToHtml(esc·표보존).
- GAS: v18~v22 push_safe node--check + API 마커 grep + 고정배포 3개 갱신.

## 🔴 월요일(6/22) 첫 실런 = 라이브 최종 관문
- 이메일 HTML 실발송(halcyon.public@gmail.com) 도착·서식 확인
- 텔레그램 PB 7섹션 + portfolioReturn 벤치비교 + 거시 출처 라이브 확인
- KR 17:0x / US 익일 08:0x 정시(daisy-chain) + 이메일 동시

## 열린 항목 (pending)
- kr-theses.md 투자 논리 **초안** → 사용자 확정 시 "(논리 초안)" 라벨 제거
- 리포트 내용 품질 사용자 피드백 (자동화·정합성은 완료)
