# 세션: 2026-04-24 참고지표 구현

## 작업 내용

### GAS
- `Config.js` — TREND_OP_TOTAL(AL11), TREND_PEND_TOTAL(AK21) Named Range 추가
- `Config.js` — PROFIT_CHART_START 하드코딩(`2025-10-20`) → `CONFIG.TREND.PROFIT_CHART_START`로 이동
- `Config.js` — `REFERENCE_INDICATORS` 배열 추가 (KOSPI/KOSDAQ/SPX 등 14개 지표 정의)
- `Config.js` — SHEET_NAMES에 `참고지표`, `참고지표_히스토리` 추가
- `Trend.js` — OP_TOTAL/PEND_TOTAL을 하드코딩에서 Named Range 방식으로 변경
- `KIS_API.js` — `getDomesticIndex()`, `getOverseasIndex()`, `getDomesticFutures()` 추가
- `KIS_API.js` — `getNearestKospi200Code()` — 코스피200선물 최근월물 코드 자동 계산
- `MobileAPI.js` — `mobileGetReferenceIndicators()` 추가 (iOS 진입점)
- `MobileAPI.js` — `_getYahooFinanceQuote()` 추가 (ES=F, NQ=F 선물 조회)
- `MobileAPI.js` — GOOGLEFINANCE fallback 로직 (`_fillGoogleFinanceIndicators`, `_fillMissingWithGoogleFinance`)
- `MobileAPI.js` — `참고지표_히스토리` upsert 로직 (`_upsertIndicatorsHistory`)
- `Main.js` — `setupNamedRanges()`에 TREND_OP_TOTAL, TREND_PEND_TOTAL 추가
- `Main.js` — `runFullUpdate()`에 `updateReferenceIndicators()` 통합
- `Main.js` — 메뉴에 "🌐 참고지표 갱신" 추가

### iOS
- `PortfolioModels.swift` — `ReferenceIndicator`, `IndicatorsResponse` 구조체 추가
- `ScriptAPIService.swift` — `getIndicators()`, `callIndicatorsFunction()` 추가
- `PortfolioViewModel.swift` — indicators, isLoadingIndicators, indicatorsUpdatedAt, indicatorsError 추가; `fetchIndicators()` 추가
- `IndicatorsView.swift` — 신규 파일. 카테고리별 섹션 카드 UI
- `MainTabView.swift` — 탭 4개로 확장 (참고지표·대시보드·종목·분석), 플로팅 캡슐 탭바로 변경

### 이전 세션에서 함께 진행된 내용
- pull-to-refresh 전체 제거 (CancellationError 문제)
- NavigationStack 제거 → 커스텀 헤더로 대체 (PageTabViewStyle 충돌)
- HoldingCard 터치 영역 전체 확장 (`.contentShape(Rectangle())`)
- GitHub repo 생성 (ejunwon-lab/FD5to6, private)
- "동기화해" 키워드 확립

## 주요 결정

- **GAS 방식 채택** — KIS API로 지표 조회 후 Google Sheets에 저장, iOS는 GAS를 통해 읽음
- **2시트 구조** — `참고지표`(현재값 요약) + `참고지표_히스토리`(날짜별 누적)
- **Yahoo Finance** — GOOGLEFINANCE가 미지원하는 ES=F, NQ=F 선물은 Yahoo Finance API 사용
- **GOOGLEFINANCE fallback** — KIS 해외지수 실패 시 INDEXSP:.INX 등으로 자동 보완
- **코스피200선물 코드** — `101W` + YYMM 형식, 분기물(3/6/9/12월) 기준 자동 계산

## 배포
- GAS: `python3 apps-script/push_safe.py` — 완료 (2026-04-25)
- iOS: XcodeGen 실행 완료. **Xcode 빌드 미확인** (에러 발생, 원인 미파악)

## 이월 → pending.md
- iOS 빌드 에러 해결
- KIS 해외지수 API (`getOverseasIndex`) 실제 동작 확인 필요
- KIS 국내선물 API (`getDomesticFutures`) 실제 동작 확인 필요
- Google Sheets Named Range 설정 실행 필요 (🛠️ 시스템 관리 → Named Range 초기 설정)
- 지표 데이터 전체 확인 (금/WTI COMEX·NYMEX 심볼 동작 여부)

## 알려진 이슈
- KIS 해외지수 조회 실패 가능성 높음 (tr_id HHDFS00000300이 지수에 맞는지 미검증)
- 코스피200선물 KIS API 미검증 (FHMIF10000000 + 101WYYMM 형식)
- VIX는 정상, TNX는 값이 ×10으로 표시됨 (TNX 특성 — 4.3% 금리 = 43으로 표시, 정상)
