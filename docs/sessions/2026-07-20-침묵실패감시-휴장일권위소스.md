# 2026-07-20 — 침묵실패 감시 묶음 + 휴장일 권위 소스 (개선 아이디어 10건 실행)

## 발단
사용자 요청: "냈어야 했는데 못 낸 개선 아이디어 10개" → 그중 2·3 우선 진행 → 5·9·1 추가 진행. 공통 주제: **사고를 그 자리에서 고치는 데서 멈추지 않고, 사고 클래스를 닫는 구조로 올라가기** ([[feedback_reframe_before_options]]).

## 완료 (5건 구현 + 1건 종결)

| # | 산출물 | 핵심 |
|---|---|---|
| 2 watchdog | `watchdog.yml` + `scripts/watchdog_check.sh` + `docs/plans/2026-07-19-자동화-watchdog.md` | 매일 21:10/22:10 KST 5종 대사 → 텔레그램 heartbeat(매일 발송 = 부재가 사망 신호). 휴장 판정은 GAS 에코 관측(달력 중복 없음). dry_run 라이브 스모크 통과 |
| 3 스캐너 | `scripts/git-hooks/pre-commit` (+run.sh 자동 활성화) | 금액(md)·시크릿(전 파일)·금지 경로 커밋 차단. 자가 테스트 8/8, 소급 FP 0 |
| 9 만기 추적 | `scripts/check_pending_due.sh` + pending `⏰` 규약 | 실행@ 때 만기 자동 표시. 첫 실행에서 당일 만기 4건 적중 |
| 5 타입 계약 | `scripts/check_sheet_types.py` (+backup_sheets.py 연동) | `_raw.json` 5시트 assert — % 문자열·주말/휴장일 행 등 6클래스 합성 검출 |
| 1 휴장일 | Holidays.js 2곳 + `docs/plans/2026-07-20-휴장일-권위소스.md`, push_safe 배포 | 화이트리스트 → 캘린더 '공휴일' 분류(ics 280건 실측). **지방선거일 6/3 잠복 결함 발견**(errors.md 2026-07-20). 월간 동기화 전환 |
| 10 재시드 | — | watchdog이 체인 사망 일일 보고 → 별도 장치 불필요, 종결 |

## 프레임 전환 기록 (1번)
- 당초 KRX API 전환으로 냈으나, 공개 ics 실측에서 구글 자체 분류(DESCRIPTION '공휴일'/'기념일')가 제헌절·지방선거일·노동절을 전부 정확히 커버함을 확인 → **신규 외부 의존 0으로 같은 목적 달성**. KRX는 GAS egress 사전 검증 불가 + 기존 고정휴장 루프가 커버라 기각. desc 부재 시 구 화이트리스트 폴백이라 최악의 경우 = 현행(회귀 불가능).

## 결정·정책
- **6번(Secret.js)**: 사용자 지시 — 백업 제안·안내 포함 **일절 금지**. memory `feedback_gas_secret` 강화·미러. 아이디어 목록에 재등장 금지.
- 7(신선도 배지)·8(onEdit 재계산) 보류, 4(GAS 테스트 하네스)는 다음 GAS 순수 로직 수정 때 점진 도입.
- watchdog 수동 dispatch는 dedup에서 제외(schedule만 집계) — 낮 테스트가 저녁 heartbeat를 억제하지 않음.

## 잔여 (pending ⏰ 등록됨)
- ⏰07-21 watchdog 첫 정기 heartbeat / ⏰07-26 휴장일 시트 실측(지방선거일 행 생성 = 분류 필터 실증; 7/25 04:00 트리거 자동) / ⏰07-27 타입계약 실백업 1회 확인
- [확인 필요] 주 맥북 백업 덤프 *현재가_이력* 2026-06-03 행 유무 — 지방선거일 잠복 결함의 실제 발현 여부 판정

## 커밋
f2a6466(watchdog+스캐너) · 3c4b65e(dry_run·dedup 보강) · 0ffc825(휴장일·타입계약·만기추적) · 7e89582(memory 미러)
