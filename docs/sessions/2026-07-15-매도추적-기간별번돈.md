# 2026-07-15 — 매도 종목 What-if 추적 + 기간별 번 돈 (v1)

## 배경
수익 표시에서 확정수익 반영 방식을 확인하던 중, 사용자가 두 기능 요청:
1. 기간별 수익 등에 번 돈(실현) 반영
2. 매도 종목을 별도 시트로 모아 매일 당일가격 기록 → "안 팔았다면 손익" vs "판 것 대비 차이"

## 결정 (AskUserQuestion)
- 기간별 번 돈 타일 추가 · What-if 과거 전체 소급 · 일별 이력 append · 시트+웹+데스크

## 핵심 발견 (설계 저위험화)
- `updateNewPriceHistory`가 *거래_원장* 전체 코드에서 종목 추출 → **판 종목도 매일 *현재가_이력*에 기록**(백업 실측 확인: 어제 4건·옛 매도·해외 MU 열 존재, 시작 2026-01-11).
- ⇒ KIS 신규 호출·별도 append 시트 불필요. **기존 시트 파생**으로 구현. 일별 이력 SSOT = *현재가_이력*, *매도추적*은 오늘 스냅샷(state).

## 구현
- **GAS** `SoldTracker.js`: `buildSoldTracker`(파생, `updatePositionFromLedger` 말미 훅) · `newMobileGetSoldTracker`(read 16필드) · `_soldLatestPriceMap` · `_setupSoldTrackerSheet`. NS.SOLD_TRACKER.
  - 국내만 what-if(해외 환율 미반영 스케일 불일치 → blank), 예금 코드 빈칸 자동 제외.
  - 산식: 안팔았다면 = 현재가×수량−매입원가 / 차이 = 안팔았다면−실현 = (현재가−매도가)×수량+수수료.
- **웹**: `periodProfit.ts`(computePeriodProfits) + 대시보드 기간별 타일 + `SoldTrackerCard`.
- **데스크**: DataProvider `useSoldTracker` + Activity `SoldTrackerPanel`.

## 검증
- vitest 49통과(periodProfit 6) · web/desk tsc·빌드 · node --check 11.
- walk-through 396500: 안팔았다면 [금액] / 실현 [금액] / 차이 +[금액] ✓.
- 배포: push_safe HEAD + gas_redeploy **v27**(고정 3배포·마커 `buildSoldTracker` 확인).
- 🔴 사용자 확인: ⚡ 후 *매도추적* 시트 생성·값 채움 / 저장! 후 웹·데스크 카드 라이브.

## 잔여 (pending)
- v2 곡선 소급: 2026-01-11 이전 매도의 매도일~현재 일별 곡선(KIS 국내 일봉 소급). 스냅샷은 전량 커버 — 효용 확인 후 결정.
