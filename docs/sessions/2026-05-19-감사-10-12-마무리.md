# 2026-05-19 — 감사 #10·#11·#12 + 진단 도구 + 메뉴 개편 (감사 마무리)

## 완료 작업

### 진단 도구 — 메뉴 방식으로 결론
- 라이브 검증 경로 논의: 공개 doGet 엔드포인트 vs 사용자 트리거 메뉴. 보안·자동화 트레이드오프 길게 검토
- 결론: **메뉴+팝업+붙여넣기** — 공개 엔드포인트 없음, 키 없음, Secret.js 무관
- `Diag.js` 신규 — `runDiag`(메뉴 핸들러, alert 팝업)·`_buildDiag`(최소 진단 빌더)
- 반환: 날짜·참거짓·개수·지표 충족도·마지막 갱신 상태. 금액·종목명·계좌·수량 절대 포함 X
- 메모리: `feedback_gas_secret.md` 강화("무슨 일이 있어도 Secret.js 안 건드림")

### 메뉴 구조 개편
- **📊 뉴시스템** (데이터): 전체·현재가·보유현황·대시보드 갱신 + 휴장일 동기화
- **🛠️ 유지보수** (운영): 🔍 진단 + ⏰ 자동 트리거 등록/해제

### 감사 #10 — vitest + CI 연동
- `web/`에 vitest 설치, `npm test`/`test:watch` 스크립트
- 테스트 작성: `changeLabel.test.ts`·`format.test.ts` — **27 케이스 전부 통과**
- GitHub Actions `deploy-web.yml`에 `Test (vitest)` 단계 추가 — push마다 자동, 실패 시 배포 차단

### 감사 #11 — push_safe.py 사전 검증
- `node --check`로 *.js 문법 + `appsscript.json` JSON 유효성 검사 후 push
- 실패 시 어디서 깨졌는지 stderr 출력 + push 중단
- node 없는 환경엔 경고 + 건너뜀 (graceful fallback)

### 감사 #12 — 레거시 디렉토리 제거
- `apps-script/`(구 GAS v1)·`ios/`(구 iOS) `git rm -r` — **65 files, 10125 deletions**
- 활성 코드 참조 0 확인 후 제거. git 히스토리 보존 (복원 가능)
- 동반 정리: `.gitignore` 6줄, `code-map.md`, `pending.md`, `CLAUDE.md` 디렉토리 도표

### 감사 #7 — 보류
- 17:30 트리거 비거래일에도 실행 (추이 기록 노이즈) — 사용자 결정으로 보류

## 변경 파일
- GAS: `Diag.js`(신규)·`Main.js`(메뉴 개편)·`push_safe.py`(syntax_check) (배포 완료)
- Web: `package.json`·`package-lock.json`·`utils/changeLabel.test.ts`(신규)·`utils/format.test.ts`(신규)
- CI: `.github/workflows/deploy-web.yml` (vitest 단계)
- 문서: `code-map.md`·`api-reference.md`·`pending.md`·`CLAUDE.md`·`.gitignore`
- 메모리: `feedback_gas_secret.md` 강화
- 삭제: `apps-script/`·`ios/` 전체

## 신시스템 감사 종합 (11/12 완료, 1건 의도적 보류)
| | 항목 | 결과 |
|---|---|---|
| #1 | 비거래일 buildDashboard 빈칸 | ✅ |
| #2 | 휴장일 2027+ 누락 | ✅ *휴장일* 시트 단일 소스 |
| #3 | 휴장일 4곳 중복 | ✅ |
| #4 | 계산 로직 이원화 | ✅ E안: StockMetrics.js |
| #5 | KIS 부분 실패 침묵 | ✅ 재시도 + carry-forward |
| #6 | 자동 트리거 실패 비가시 | ✅ 대시보드 상태줄 |
| #7 | 비거래일 트리거 노이즈 | ⏭️ 보류 |
| #8 | 중복 함수 정의 | ✅ |
| #9 | 일회성 함수 잔존 | ✅ |
| #10 | 테스트 0 | ✅ vitest + CI |
| #11 | push 문법 검증 없음 | ✅ |
| #12 | 레거시 디렉토리 | ✅ |

## 남은 일
- 사용자: 시트 새로고침 → 두 메뉴(뉴시스템·유지보수) 확인. "이상해" 시 🔍 진단 → 팝업 복사 → 채팅에 붙여넣기
- 로컬 `apps-script/`·`ios/` 폴더 잔여 잡파일은 사용자가 Finder에서 직접 정리 (repo엔 영향 없음)
