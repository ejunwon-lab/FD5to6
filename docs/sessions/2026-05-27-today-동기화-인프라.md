# 2026-05-27~28 — Today 페이지 + 클라이언트 동기화 + 배포 인프라 보강

## 작업 5건

### A. 데스크 Today 페이지 신규 (commit `2d29d75`)

**위치**: Workspace > Today (단축키 **Y**, Dashboard 다음 자리)

**디자인**: 시안 A — 막대 리스트 (Bloomberg/IBKR 풍). 사용자 선택.

**구성**:
1. KPI 4칸: 포트 변동 (₩+%) · ▲상승 · ▼하락 · ⏸보합
2. Sort 토글: 등락률·₩등락액·종목명 / Filter 토글: ALL·GAIN·LOSS·FLAT
3. 종목별 막대 카드 (정렬 결과 순서):
   - Row 1: `#순위 ▲/▼/⏸ 종목명 코드 KR/US ··· +X.XX%`
   - Row 2: `현재가 · +N원/주 × M주 · +N×M원`
   - Row 3: 가로 막대 (길이 = `|dayChangePct| / maxAbsPct`, 색 = gain/loss/neutral)

**FLAT_THRESHOLD**: `0.05%` 미만은 보합

**파일**: `web-desk/src/components/today/TodayPage.tsx` 신규 · `Sidebar.tsx` NavKey + 항목 · `App.tsx` SHORTCUTS + case

### B. GAS 시계열 휴장일 drop (commit `1ebee29`)

**문제**: 대시보드/데스크 차트에 토·일·공휴일 행이 표시되어 시각 노이즈 + Benchmark 차트 등에서 date 매칭 어긋남

**해결**: 기존 `_isTradingDateStr(s)` helper (Holidays.js *휴장일* 시트 + 토/일 체크) 재활용. 3 endpoint에 filter:
- `newMobileGetProfitHistory` (추이 기록 → equityCurve)
- `newMobileGetIndicatorHistory` (참고지표_히스토리)
- `newMobileGetStockDetail` priceHistory (안전망)

**효과**: 4 클라이언트 (web-desk·web·iOS·Telegram) 자동 혜택. 시트는 그대로 보존. errors.md 2026-05-17 패턴 확장.

### C. 클라이언트 호환 동기화 (commit `375cc0d`)

**문제 1**: GAS `newMobileGetMonthlyRealized` 응답이 *행 단위 entries* 로 바뀐 뒤 web (PWA)·iOS 가 `monthly` 키를 기대했지만 못 받아 Analysis 화면 깨짐

**해결**:
- GAS 응답에 **`entries[]` (행 단위, 데스크) + `monthly[]` (집계, web·iOS) 둘 다** 포함 (후방 호환)
- 클라이언트 코드 변경 0

**문제 2**: web·iOS HoldingCard 우측이 *현재가 + 등락률* 만 있어 데스크의 *등락액 식* 정보 미반영

**해결**:
- `web/src/components/holdings/HoldingCard.tsx`: 우측에 `+500원 / +0.66%` + `+500원 × 30주 = +15,000원` 두 줄 추가
- `ios2/Sources/Features/Holdings/HoldingCard.swift`: 같은 정보 SwiftUI 두 줄 추가

### D. 배포 인프라 사고 + 수정 (commit `41bd8b3`)

**사고**: `deploy-web` workflow 수동 trigger 후 `https://ejunwon-lab.github.io/FD5to6/desk/` 가 404. **`/desk/` 폴더 통째로 삭제됨**.

**원인**: `peaceiris/actions-gh-pages@v4` 의 *기본 동작*은 publish_dir 내용으로 gh-pages 브랜치 전체 덮어쓰기. `deploy-web-desk.yml` 은 `keep_files: true` + `destination_dir: desk` 로 *desk/ 하위만 갱신·기존 보존*. 그러나 `deploy-web.yml` 은 `keep_files` 옵션 없이 *gh-pages 루트 전체 덮어쓰기* → desk/ 삭제.

**해결**:
- `deploy-web.yml` 에 `keep_files: true` 추가 → 향후 web 배포해도 desk/ 보존
- `deploy-web-desk` workflow 수동 trigger → `/desk/` 복원

**Bonus**: 사고 직전 `peaceiris/actions-gh-pages@v4` 다운로드 `codeload.github.com` 일시 장애로 3회 fail → 시간 두고 재시도하니 성공. *GitHub 인프라 일시 장애 패턴* 인지.

### E. Footer 라벨 정리 (commit `7b3161a`)

데스크 Footer 의 Bloomberg 풍 장식 라벨 (`F1 HELP · F2 QUOTE · F3 NEWS · F4 CHART · F5 REFRESH · / SEARCH`) 을 사이드바 단축키 첫글자 패턴과 일관성 있게 교체:

```
D DASHBOARD · Y TODAY · H HOLDINGS · A ANALYSIS · I INDICATORS · T TRADE LOG
```

같은 commit 에 `pending.md` 정리 — *web/ 숫자 풀 출력* 항목은 `krwCompact ≡ krwFull` 로 *이미 적용* 된 상태였음 (명시적 축약 표기 0건 grep 확인).

## 검증

### 자동 통과 ✓

- GAS push (Secret.js 보호): 모든 변경 commit 마다 ✓
- web·web-desk tsc + build: 모든 단계 success
- GH Actions deploy-web-desk·deploy-web: success (codeload 회복 후)
- Production hash 일치:
  - `/desk/` → `index-Cl0mdci3.js` (마지막 build)
  - `/` (web) → `index-B1y6_Y4S.js`

### 🔴 사용자 검증 대기

1. **Today 페이지** (단축키 Y) — KPI 4칸 + 막대 카드 정렬·필터 정상
2. **휴장일 drop** — Dashboard ProfitChart·Analysis BenchmarkPanel·Monthly Heatmap·Indicator Detail Modal·종목 상세 priceHistory 차트 X축에 토·일·공휴일 안 보임
3. **Footer 라벨** — 데스크 하단에 D/Y/H/A/I/T 단축키
4. **web HoldingCard 등락액 식** — 카드 우측 3줄 (현재가 / +N원/+X.XX% / +N × M주 = +NM원)
5. **iOS HoldingCard 등락액 식** — *Xcode 빌드 + 기기 설치 필요* (사용자 작업)

## 다음 작업 후보

- **Phase C 진입** (드로다운 곡선·상관관계 매트릭스·이코노믹 캘린더·배당·CSV·거래빈도). GAS 큰 확장 필요. 6~9시간 분량
- **Placeholder 페이지 채우기** (Price History · Dividends · KIS Status · Settings)
- **Phase B 잔여 1건 — 섹터 도넛** (*분류 매핑* 시트 신규 후)
- **iOS Phase 3** (mobileGetPortfolio → 포지션/가격_히스토리 기반 전환)
- **Phase B 패널 7개 사용자 검증** (실 데이터로 점검)
