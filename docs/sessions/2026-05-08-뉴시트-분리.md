# 2026-05-08 신시스템 신규 구글 시트 분리

## 작업 내용

### 1. 마이그레이션 함수 (`apps-script/NewSystem.js`)
- `migrateToNewSpreadsheet()` 추가 — 새 구글 시트 자동 생성 + 데이터 시트 복사
  - 복사: `*거래_원장*`, `*현재가_이력*`, `*실현손익*`
  - 신규 생성: `*거래_입력폼*`, `*보유현황*`, `*설정*`
- `_setupSettingsSheet()` 추가 — FX 환율 시트

### 2. `apps-script-v2/` 신규 GAS 프로젝트 생성
구 시스템 의존성을 완전히 제거한 독립 GAS 프로젝트.

| 파일 | 변경 내용 |
|---|---|
| `NewSystem.js` | `updateNewPriceHistory()` — `getTrackerActiveData` 제거, KIS API 직접 조회 |
| `NewSystem.js` | `updateFxRates()` — GOOGLEFINANCE로 USD/GBP 자동 갱신 |
| `NewSystem.js` | `importHistoricalTrades()` — 과거 이력 재추가 |
| `Dashboard.js` | `_getFxRates()` — `*설정*` 시트(B2/B3)에서 읽도록 변경 |
| `Main.js` | 신시스템 전용 메뉴 (트리거·초기화 메뉴는 설정 후 제거) |
| `push_safe.py` | Script ID: `1DC8llpWYz2ZvzsqVCaz60qomATwxP_CBzuHBitCf0uQT5NbBF-n7IHdZ` |

### 3. 트리거
매일 18:00 `scheduledDailyUpdate` → `updateAllNew()` (FX + 현재가 + 보유현황 + 대시보드)

### 4. 메뉴 최종 구성
- 전체 업데이트 (가격 + 보유현황 + 대시보드)
- 현재가만 업데이트
- 보유현황 재계산
- 대시보드 갱신

## 완료 항목
- 새 구글 시트 생성 및 GAS 배포 완료
- 보유현황 / 실현손익 / 대시보드 정상 생성 확인
- 매일 18:00 트리거 설정 완료

## 잔여
- Secret.js 새 시트 GAS에 수동 복붙 (사용자가 직접)
- iOS 연결 (Phase 3 — 나중에)
