# 세션: 2026-04-25 버그수정 및 지표개선

## 작업 내용

### GAS
- `MobileAPI.js` — `_IS_MOBILE_CALL` 중첩 호출 버그 수정 (finally에서 false 고정 → 이전 값 복원)
- `Config.js` — 금/WTI/달러인덱스 소스를 GOOGLEFINANCE → Yahoo Finance로 변경 (`GC=F`, `CL=F`, `DX-Y.NYB`)
- `Config.js` — SOX에 Yahoo Finance fallback 추가 (`^SOX`)
- `MobileAPI.js` — `_fillMissingWithYahooFinance()` 함수 추가 (KIS 실패 시 ySymbol로 Yahoo Finance 조회)
- `KIS_API.js` — 코스피200선물 API 응답 Logger 추가 (디버그용)

### iOS
- `MainTabView.swift` — 초기 탭을 0(참고지표) → 1(대시보드)로 변경
- `CacheService.swift` — 참고지표 캐시 추가 (`saveIndicators`, `loadIndicators`)
- `PortfolioViewModel.swift` — 앱 시작 시 참고지표 캐시 즉시 로드
- `PortfolioViewModel.swift` — fetchIndicators 성공 시 캐시 저장
- `IndicatorsView.swift` — 자동 fetch `.task` 제거 (캐시 데이터 표시 후 수동 갱신)
- `IndicatorsView.swift` — `.refreshable` 제거

## 이월 → pending.md
- 코스피200선물 KIS API 응답 구조 확인 필요 (Logger 추가해뒀음 — GAS 실행 후 로그 확인)
- iOS 빌드 에러 미해결 (SourceKit 에러는 false positive, Xcode 실제 빌드 확인 필요)
