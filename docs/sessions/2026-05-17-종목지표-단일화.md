# 2026-05-17 — 감사 #4: 종목 지표 계산 단일화 (E안)

## 배경
감사 #4(계산 로직 이원화) 해결. `_mCalcExtras`(앱)·`_calcExtraColumns`·`_calcTodayProfit`
(시트)이 *현재가_이력*에서 같은 계산을 따로 구현 → 드리프트(감사 #1 버그가 그 사례).

## 결정 — E안 (decisions.md 참조)
종목 지표를 `computeStockMetrics`가 한 번 계산해 *종목지표* 시트에 저장,
앱·대시보드는 읽기만. *보유현황*이 이미 "미리 계산해 저장한 표"인 패턴과 동일.

## 완료 작업

### 신규
- `StockMetrics.js` — `computeStockMetrics`(지표 계산→*종목지표* 시트),
  `_readStockMetrics`(시트→Map, 없으면 1회 자동 계산), `_setupStockMetricsSheet`
- *종목지표* 시트 (NS.STOCK_METRICS) — 종목별 당일등락·1주/1달 손익·1M~1Y%·52주

### 삭제 (computeStockMetrics로 통합)
- `_mCalcExtras` (MobileAPI.js) · `_calcExtraColumns`·`_calcTodayProfit` (Dashboard.js)
- 호출처 각 1곳뿐임을 확인 후 삭제 (newMobileGetStockDetail은 독립 — 영향 없음)

### 배선
- `newMobileGetPortfolio`·`_mMapHolding`·`buildDashboard` → *종목지표* 읽기로 전환
- `updatePositionFromLedger` 끝에 `computeStockMetrics()` 호출 → 모든 갱신 경로
  (updateAllNew·newMobileUpdate*·메뉴)가 자동 통과 (R1 낡음 리스크 차단)
- 클라이언트(web/iOS) 무변경 — API 응답 구조 불변

## 변경 파일
- GAS: `StockMetrics.js`(신규)·`MobileAPI.js`·`Dashboard.js`·`NewSystem.js` (배포 완료)
- 문서: `code-map.md`·`api-reference.md`·`decisions.md`

## 남은 일
- 사용자: GAS `📋 보유현황 재계산`(또는 🔄 전체 업데이트) 1회 실행 → *종목지표* 시트 생성
- 감사 #5~#12 사용자와 하나씩 상의 (다음: #5 KIS 부분 실패 침묵)
