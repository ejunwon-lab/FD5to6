# 2026-08-06 — 데스크 iPad 개선 트랙 (사이드바 → Today 전면 개편 → 계좌 뷰 → Pages 장애)

iPad 가로모드 사용을 전제로 한 데스크 연쇄 개선 세션. 사용자 피드백을 받아가며 8건 배포.

## 1. 사이드바 재편

- 문제: Workspace(6)/Data(2)/System(2) 분류 무의미, Trade Log 이름·내용 불일치, Dividends placeholder, 터치 타깃 미달
- 적용: **Portfolio/Activity/Market/System 4섹션**, NavKey `tradelog`→`activity`, Dividends 메뉴·`v` 단축키 삭제(기능은 로드맵 백로그 유지), 행 `py-2.5`(~40px), 단축키 힌트 `pointer-fine` 한정(tailwind 커스텀 variant)

## 2. 홀딩스 카드 폴드 정리

- 기기 불문 **항상 접힘 시작** (Settings cardFoldDefault 옵션·resolveFoldDefault 삭제)
- 그리드 `items-start` — 펼친 카드 옆 접힌 카드가 행 높이만큼 늘어나던 문제 해소
- **아코디언**: 접힘 모드에서 새 카드 펼치면 기존 펼침 자동 접힘(foldOverrides 단일 교체). '모두 펼치기' 모드는 개별 토글 유지

## 3. Today 전면 개편 (인사이트 9종)

- KPI 6칸: 포트 변동·Breadth 큰 타일 + **KOSPI·KOSDAQ·S&P500·USD/KRW** 컴팩트 타일(lg 1.4fr:1fr)
- `lib/todayInsights.ts` (vitest 10) — 계좌→종목 합산·주의 신호(수익↔손실 전환·±3% 급등락·52주 근접)·90일 분포 백분위
- 패널: 기여도 Top5±·주의 신호·시장/계좌 분해(+오늘 위치)·**뉴스**·**이벤트**
- 오늘 실현손익 조건부 스트립, iPad 2단 레이아웃(좌 리스트/우 인사이트), 계좌 필터 칩(대시보드와 동일)

## 4. 뉴스 + 이벤트 캘린더 (외부 API — /design-check 통과)

- 설계 노트: `docs/plans/2026-08-06-Today-뉴스-이벤트캘린더.md` — 로컬 실측으로 소스 확정
- **GAS News.js** `newMobileGetNews`: 네이버 국내(기사 직링크)/해외(한국어 번역, 뉴스탭 링크) fetchAll + 30분 캐시. Yahoo RSS는 실측 사망으로 배제
- **US 실적일**: quoteSummary crumb 락 → 쿠키→getcrumb 2단 인증 로컬 실측 성공(MU 9/23) → GAS `_newsUsEarnings_` 이식, `earnings[]` 동승
- **매크로 캘린더** `lib/macroCalendar.ts` (vitest 7): FOMC·CPI·금통위 웹 검증 정적 일정 + 만기일 규칙 계산(KR 둘째 목·US 셋째 금). ⚠️ 연도 갱신 필요
- 데스크: `useNews` 공유 훅(inflight dedup), NewsPanel(실패/빈 구분 = GAS→네이버/Yahoo 실환경 스모크 신호), EventsPanel(D-day 병합)

## 5. 홀딩스 카드 가독성 (스크린샷 피드백)

- 헤더 3줄 라벨링(현재가·{changeLabel} 등락·손익 — 접힘 헤더 포함)
- 매입/평가/수익 3열 겹침 → **매입·평가 윗줄 2칸 + 수익 아랫줄 중앙**. Web·Terminal 공통

## 6. 계좌 현황 강화

- `AccountDetailModal` — Account P&L 계좌 행 탭 → KPI 6칸(평가·원금·수익·오늘·실현누적·대기자금)+종목 표+실현 최근 8건. 추가 GAS 호출 없음, broker+계좌 복합키
- Account P&L 상세 표: 순서 누적 → 라디오 → **항상 하나 열림** + UX 반복 개선(탭 바 시도 → 사용자 피드백으로 **외곽선 연결** 확정: 선택 카드 accent 외곽선+옅은 배경, 상세 박스 같은 색 외곽선, ▼ 꼭지 → **카드 전폭 브리지**로 교체)

## 7. GitHub Pages 서빙 빌드 장애 (미해결 — 대기 결정)

- 마지막 배포(브리지 UI, 9a337b9)가 gh-pages까지 정상 도달했으나 **Pages 서빙 빌드 4연속 실패** — 콘텐츠 무관 리포 단위 GitHub 장애. 재빌드 POST·넛지 커밋·워크플로 재실행 전부 무효. errors.md 8/6 기록(진단 경로 + 함정 2개)
- 사용자 대기 결정 — 다음 세션 절차는 pending.md 참조 (회복 확인 or Cloudflare 이전 착수)

## 8. 작업 방식 피드백 (memory 저장)

- `feedback_no_blocking_waits` — 배포 후 CDN 전파·Pages 빌드를 폴링으로 붙잡지 말 것. push+워크플로 성공 확인까지만 하고 보고 종료

## 검증 요약

- vitest 25/25 (신규 17: todayInsights 10 + macroCalendar 7) · tsc·build 전 커밋 클린 · GAS push_safe 2회 배포
- 🔴 사용자 확인 잔여: pending.md "데스크 iPad 가로모드 개선 트랙" ⓐ~ⓓ + Pages 회복 확인
