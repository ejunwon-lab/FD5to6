# 2026-04-26 멀티기기 세팅

## 작업 내용

### 두 기기 작업 환경 구성
- 사무실 맥북 (halcyon) + 집 Mac mini (neoicarus2) 간 이어서 작업하는 흐름 설계 및 세팅

### Mac mini 초기 세팅 완료
- AirDrop 복사본에 .git 포함되어 있어 git remote 이미 연결된 상태 확인
- `gh auth login`으로 GitHub 인증 완료 (Google 계정 → 브라우저 인증)
- Claude 메모리 복원: `FD5to6/memory/*` → `~/.claude/projects/-Users-neoicarus2-.../memory/`

### CLAUDE.md 수정
- `실행@` 키워드 추가: git pull + memory 복원 + 현황 요약
- `실행@` 상세 절차 섹션 추가
- 경로 오타 수정: `Claude-2026-FD5to6` → `Claude-2026-mini-FD5to6` (3곳)
- 하드코딩된 `halcyon` 경로 → `$(whoami)` 동적 처리로 변경

## 확정된 작업 흐름

| 시점 | 명령어 |
|---|---|
| 작업 시작 | `실행@` |
| 작업 종료 | `저장!` |

어느 기기, 어느 장소든 동일.
