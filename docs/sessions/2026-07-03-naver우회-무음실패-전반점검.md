# 2026-07-03 — Naver 차단 우회 · 무음 실패 알림 · 전반 점검

## 1. Naver 차단 14일 해소 (KR 리포트 데이터 복원)

- **진단이 가설을 뒤집음**: "러너 IP 차단" 가설로 GAS/KIS 대공사를 설계했으나, `diag-egress.yml`(신규, dispatch 전용 진단 워크플로) 실측 결과 **러너 curl은 Naver 전부 200** — 차단의 실체는 Claude CLI **WebFetch 도구 계층**(robots 정책 추정). `/design-check` 게이트 첫 적용 사례: 코딩 전 진단 배치 덕에 A-2a/2b(GAS marketSnapshot·KIS TR 확장) 전체가 불필요함이 판명.
- **수정**: kr-prompt — Naver·뉴스를 Bash curl로(브라우저 UA, finance.naver.com은 iconv euc-kr), 죽은 URL 교체(한경 /finance 404, 인포맥스 articleList 빈응답→RSS), 종목뉴스 API(`m.stock.naver.com/api/news/stock/{code}`) 채택. us-prompt — 인포맥스 RSS 교체 + WebFetch 실패 시 curl 폴백(Yahoo는 WebFetch 유지).
- **게이트 통과** (dry_run 28652217934): 거래대금 45.5조/6.9조, 업종 +8.96%, 시장·종목 수급, "차단" 문구 소멸.
- 부수 확인: Yahoo는 러너 curl에서 429 잦음 → pending ⓐ(KOSDAQ 주간% 변동) 원인. curl 재시도 지시 추가.

## 2. 주간 리포트 GAS pre-fetch 구조 전환

- 6/28 첫 라이브에서 에이전트가 env 주입돼 있는데 "환경변수 미설정" 오판 → 역산 추산값 사용. **해결**: weekly job에 pre-fetch step(워크플로 curl → `/tmp/weekly/gas_metrics.json`, 공개 로그에는 HTTP 코드·필드 유무만) + 프롬프트 파일 Read 1차/env curl 폴백.
- **게이트 통과** (dry_run 28652226683): `pre-fetch: HTTP=200 success=true dailyReturns=true`, 일자별 실값(月+1.21%~金+1.73%). 에이전트가 d5 +6.4%의 입금 왜곡(귀속 합산 ≈ -0.5%)을 스스로 플래그 — C 필요성 재확인.

## 3. 전반 점검 (탐색 에이전트 3 + 로컬 검사)

- 건강: web 테스트 34/34, 두 앱 tsc 클린, GAS 핵심 경로(트리거·doPost·텔레그램) 에러 처리 견고.
- 발견(미처리분): GAS 죽은 코드 클러스터(모바일 GET 8함수 + KIS 절반 — **Secret.js 원격 확인 전 삭제 금지**), 환율 fallback 불일치(0 vs 1400/1700), buildDashboard 셀 단위 I/O, 두 웹앱 포맷/52주/시간대 드리프트, TopBar KST 오표기, save.sh 트레일러 모델명, npm install→ci, web 테스트 공백 4건, pending 완료항목 잔존.

## 4. P1 즉시 수정 (사용자 승인)

- 데스크 표시규칙: EquityChart Y축 "M"·StockDetailModal "k" 축약 제거(toLocaleString 풀), AnalysisPage Winners/Losers 종목명 메인 전환, shortKRW·compact 제거. tsc·build·Pages 배포 success.
- 무음 실패 제거: market-report 3 job에 생성 실패(found=no) 시 텔레그램 경고+exit 1 (dry_run 시 억제), telegram-push 체인 사망 시 failure() 알림. 설계: `docs/plans/2026-07-03-무음실패-알림.md`.

## 남은 것 (pending.md에 반영)

- 라이브 확인: 7/5(일) 주간(pre-fetch)·7/6(월) KR(거래대금·업종) — 관찰만.
- 사용자 결정: C 옵션(1+2 권고), Secret.js 확인(죽은 코드), kr-theses 논리 확정.
- 위생 묶음(P2~3): 웹 드리프트·환율 fallback·pending 정리·기타 — 다음 세션 후보.
